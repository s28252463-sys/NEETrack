'use client';

// This file is now empty because its content has been moved to the root src/app/page.tsx
// to serve as the main dashboard view for authenticated users. This simplifies the routing
// and resolves the infinite loading issue.

export default function DashboardRedirectPage() {
  return null;
}
