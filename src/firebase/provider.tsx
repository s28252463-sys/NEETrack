// This is a placeholder file to resolve the build error.
// Firebase provider implementation will be added in a subsequent step.
"use client";
import React from 'react';

export function FirebaseProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

export function useFirebase() {
    return {};
}
